# 虚拟手柄工程 · 结构化手册

> 由 `使用说明.txt` 重组而成。原文件按**时间倒序**追加，同一问题在多轮里被反复推翻重写（最典型：右摇杆到底用 Rx/Ry 还是 Z/Rz，前后结论相反），查东西只能全文翻。
> 本手册按**主题**重排：当前有效的结论放正文，已被推翻的历史结论单独归档到第 8 章。
>
> **代码基线**：2026-09-19 深夜版 `work_final/`（`PROTOCOL_VERSION = 5`，`GamepadView` 12758 行、`PadLayout` 3716 行，含 `assets/color_picker.html`）。
> 带 `文件:行号` 的数字都是从代码里查出来的，改代码后请以代码为准。

---

## 0. 基线速览

| 项 | 当前值 | 出处 |
|---|---|---|
| 协议版本 | `PROTOCOL_VERSION = 5` | `GpadDaemon.java:62` |
| 监听端口 | 19820（仅 127.0.0.1） | `GpadDaemon.java` |
| 手柄设备 | `Virtual Gamepad v5`，报告 13 字节 | `GpadDaemon.java` |
| 键盘设备 | `Virtual Gamepad Keys v5 (nkro)`，30 字节 bitmap | `GpadDaemon.java:145` |
| VID/PID | `1D6B:0104`（Bus USB） | `GpadDaemon.java:78-79` |
| 描述符 | 89 字节，Android CDD 布局 | `GamepadReport.java` |
| 元素下标 | 见 6.1，共 `N = 199`（末尾多了悬浮球 G） | `PadLayout.java:254-257` |
| 取色器 | `assets/color_picker.html` + WebView | `MainActivity.java:977` |
| 布局版本 | 竖 14 / 横 13 / 键盘 8 / 存档 meta 4 | `PadLayout.java:280,281,305,484` |
| 摇杆属性 | `stickRange` 0.5–3.0（默认 1.5）、`stickFixed` 默认 true | `PadLayout.java` |
| 组合键 | 16 槽 × 4 方向 × 32 动作，延迟 12 档 | `PadLayout.java:149,131,146,163` |
| 穿透方案 | touchableRegion 单窗口（反射） | `TouchRegionHelper.java` |
| 编译环境 | compileSdk 33、targetSdk 28、Java 8 | `app/build.gradle` |
| Shizuku | `dev.rikka.shizuku:*:13.1.5` | `app/build.gradle` |

**每天开机只需确认一件事**：终端横幅是 `VGPAD protocol=5`，浮层左上角是绿点「已连接 v5」。

> ⚠️ **v4 → v5 是破坏性变更**：键盘从 boot 6KRO 换成 NKRO bitmap。必须完整走一遍 `pkill` → 重装 → 重跑启动命令，否则旧进程收到新 usage 会照旧往 6 格里塞，第 7 个键照样丢（详见 3.3）。

---

## 1. 部署与启动

### 1.1 目录结构

解压后把 `work_final` 放到手机 `/sdcard/AppProjects/VirtualGamepad/`，AIDE 认 `app/src/main` 这一层：

```
VirtualGamepad/
├── build.gradle / settings.gradle / gradle.properties
├── app/
│   ├── build.gradle
│   └── src/main/{AndroidManifest.xml, java/, res/}
├── patched_files/              ← 旧版备份，不参与编译
└── 使用说明.txt
```

> `patched_files/` 里是 `FloatingService / GamepadView / MainActivity / TouchRegionHelper` 四个文件的**改动前备份**（主目录里已是改过的版本）。不要把它当源码目录拷进工程。

目录名刻意用英文：AIDE 构建链对中文路径偶发出编码问题。

### 1.2 编译

AIDE 打开 `VirtualGamepad/` 直接 Build / Run。Shizuku 那三行依赖拉不到就注释掉——只是少了「自动激活」，功能不缺。

### 1.3 启动守护进程

```
CLASSPATH=/data/app/com.example.vgamepad-xxx/base.apk \
app_process / com.example.vgamepad.GpadDaemon
```

三条路等价：电脑 adb shell / LADB / Termux 手动粘贴、app「自动激活」（Shizuku）。
命令**挂起不返回是正常的**——它在读 stdin 保活。想后台跑：

```bash
nohup CLASSPATH=... app_process / com.example.vgamepad.GpadDaemon > /sdcard/vg.log 2>&1 &
```

### 1.4 正确验证顺序（别跳步）

1. `pkill -f [G]padDaemon` ← 方括号不能省，见 5.4
2. `ps -A | grep -i [g]pad` — **必须确认无残留**
3. AIDE 重新编译安装（改过 `GamepadReport` / `GpadDaemon` 就必须重装，守护进程读的是 APK 里的 dex）
4. 回 app 点「复制命令」，重新执行（APK 路径可能变）
5. 终端依次看到：
   `Virtual Gamepad ready on 127.0.0.1:19820` → `kernel opened device: true` → `listening …` → `alive 5s, clients=0, cmds=0 (waiting)`
6. `alive Ns` 持续出现 = 活着；没有 `!! 端口已被占用`
7. 打开 app 启动手柄 → `client connected (#1)` → 浮层绿点 + `已发送 N`

想确认设备真的注册了，加 `--selftest` 跑一次（摇杆自画圈 3 秒），默认关闭。

### 1.5 自动激活三档（Shizuku）

| Shizuku 状态 | 表现 |
|---|---|
| 在跑 + 已授权 | 全自动，点一下守护进程就起来 |
| 在跑 + 未授权 | 自动发请求，你在 Shizuku 点「允许」，回来自动接着启动 |
| 没在跑 | 3 秒超时后提示，并拉起 Shizuku 让你手动启动 |

能力边界：Shizuku 服务本身必须先由用户启动一次（无线调试配对 / root），授权也必须人点一次，这是它的安全设计，绕不过。

### 1.6 退出 / 收起（四层保险）

1. 手柄顶部红色「收」按钮；
2. 悬浮球：点开展开、点「收」回球，球可拖到任意位置；
3. 通知栏常驻：[收起/展开] 和 [停止]；
4. 长按小球 = 彻底停止服务。

「收」按钮与小球**共用位置**：球拖到哪，展开后「收」就在哪，反之亦然。

---

## 2. 触摸穿透：区域方案（本节为新版核心机制）

### 2.1 为什么不用 alpha 方案

Android 12 起，只设 `FLAG_NOT_TOUCHABLE` 的悬浮窗**不再默认支持事件穿透**。官方替代路子要求把窗口 alpha 压到 0.8 以下——整个手柄被迫半透明，游戏画面从底下透上来，按键盘底发灰、字读不清，对手柄这种长时间盯着的界面基本不能用。

### 2.2 本项目走 touchableRegion

系统底层一直支持「一个窗口只有部分区域吃触摸」：`WindowState.getTouchableRegion()` 的 `TOUCHABLE_INSETS_REGION` 分支，状态栏和输入法都在用。只是这 API 是 `@hide` 的，只能反射。

做法：窗口本身可触摸，但通过反射把 touchableRegion 设成「只覆盖按键那一块」。区域之外**窗口根本不遮挡**——严格说那不是"穿透"，是窗口在那里压根不存在。

相比 alpha 方案：
- alpha 可保持 1.0，画面完全不透明
- 不受 Android 12 那条限制
- 整个手柄只需 **1 个窗口**

### 2.3 为什么不沿用「每键一个窗口」

键盘布局 80+ 个键 = 80+ 个 `TYPE_APPLICATION_OVERLAY` 窗口。系统对这类窗口有数量上限，开到几十个会被批量回收——表现是"窗口一个个消失，然后 app 崩溃"。
`MAX_PASS_THROUGH_KEYS = 24` 就是多窗口模式下的上限（`FloatingService.java:116`）。

### 2.4 失败回退

Android 9+ 有 hidden API 限制，反射可能被拦。所有方法返回 boolean 表示成败，调用方据此回退到多窗口模式，**最坏是回到原行为，不会更差**。
主界面「当前穿透模式」那行会显示实际生效的模式，一眼能判断反射成没成功。

坦白说：这个效果借的是系统内部实现，不是文档承诺的能力，不同 ROM / 系统版本随时可能失效。真失效了，主界面「强制多窗口模式」勾选框就是退路（机制简单，但按键多时更卡）。

### 2.5 提交时序（两处重试，别删）

- `REGION_COMMIT_MAX = 3`，每次间隔 `60ms`（`FloatingService.java:1303-1355`）
- `REGION_SETTLE_MAX = 5`，每次间隔 `80ms`（`:1418-1419`）

原因：region 提交和窗口重建不是同步的，一次提交可能被后续 traversal 冲掉。删掉重试会出现"加了按钮用单窗口，删掉按钮又变多窗口"的抖动。

### 2.6 三个界面按钮

「编」（进编辑）、「收」（收起）、「布」（切换布局）、「透」（穿透开关，默认隐藏）。
`isUiButton()` 判定这四个（`PadLayout.java:546-547`）——它们**不给删**（删了「编」就再也进不了编辑模式），且「透」有独立外观：扁矩形 0.89×0.53、大小 65%、字号 30%（`applyPassDefault()`）。

---

## 3. HID 映射基线（默认它是对的）

出问题先怀疑别处。这套成立靠四个条件**同时**满足：

1. `protocol = 5`；
2. 描述符按 Android CDD 排（89 字节）：X/Y 左摇杆，**Z/Rz 右摇杆**，Brake/Accelerator 做 L2/R2 扳机；
3. VID/PID = `1D6B:0104`，不带厂商 kl → 系统必然回落 `Generic.kl`（里面有 312/313 两行，L2/R2 才有按键）；
4. 设备名 `Virtual Gamepad v5`（键盘设备带 ` (nkro)` 或 ` (boot6)` 后缀），不带厂商 kl 名。

### 3.1 按钮位号

HID button N → Linux `BTN_GAMEPAD + (N-1)` → `Generic.kl` → Android KEYCODE。顺排会整体串位，所以 C、Z 两位空着跳过：

| 位号 | 按键 | 位号 | 按键 |
|---|---|---|---|
| 0 | A | 8 | R2 |
| 1 | B | 9 | (跳过) |
| 2 | (C，跳过) | 10 | SELECT |
| 3 | X | 11 | START |
| 4 | Y | 12 | MODE / HOME |
| 5 | (Z，跳过) | 13 | THUMBL / L3 |
| 6 | L1 | 14 | THUMBR / R3 |
| 7 | R1 | | |

### 3.2 轴语义

- 左摇杆 = `AXIS_X` / `AXIS_Y`
- 右摇杆 = `AXIS_Z` / `AXIS_RZ`
- L2 = `BUTTON_L2` + `AXIS_LTRIGGER`（= AXIS_BRAKE）
- R2 = `BUTTON_R2` + `AXIS_RTRIGGER`（= AXIS_GAS）

扳机**按钮 + 轴双发**，只认按钮的和只认轴的游戏都能用。

### 3.3 键盘设备：NKRO bitmap（v5 起）

- 第二个 uhid 设备，手柄描述符一个字节都没动；
- 表里存的是 **HID keyboard usage**（`0x04`='A'、`0x1A`='W'、`0x2C`=空格、`0xE0/0xE1/0xE2`=左Ctrl/Shift/Alt…），不是 Android keycode，省掉一层反查；
- **v5 起换成 NKRO bitmap 报告，30 字节**，不再有 6 键上限。

```
[0]      修饰键 bitmap（8 位，usage 0xE0..0xE7）
[1]      保留
[2..29]  28 字节 = 224 位的键 bitmap，覆盖 usage 0x00..0xDF
```

一位一个键，按下置位、松开清位，**没有"格子用完"这回事**。

**为什么原来最多 6 键**：旧版是 boot 协议的 8 字节报告——修饰键 bitmap 1 字节 + 保留 1 字节 + 普通键 Array 6 格。那个"6"是数组只有 6 格的硬限制，不是代码里写死的判断：第 7 个键 find 不到空格子，只能丢弃。

**兼容性**：bitmap 报告不符合 boot 协议（BIOS / 老系统不认），但 Android 走 Linux hid-input，4.0 之后 NKRO 已修好，照常映射成 Linux keycode，再由 `Generic.kl` 映射成 `KEYCODE_*`。
万一某台机器不认，守护进程加参数回退：

```bash
CLASSPATH=... app_process / com.example.vgamepad.GpadDaemon --kbd-boot
```

设备名会变成 `... (boot6)`，据此可确认当前跑的是哪一套。默认（不带参数）是 `(nkro)`。

**注意**：键 bitmap 声明到 `0xDF`(223) 为止，故意不与修饰键 `0xE0..0xE7` 重叠——同一个 usage 在两个 field 里各声明一份，Linux 会各映射一次，行为不可预期。

**验证**：同时按下 7 个以上键时，守护进程终端会打一行（只打一次）：
`[kbd] NKRO ok: N keys pressed at once (>6)`

### 3.4 改描述符的规矩

只有「要改报告里每个字节的含义」时才动 `GamepadReport.java`。动完必须 `PROTOCOL_VERSION +1`，然后完整走一遍：
`pkill` → 确认无残留 → AIDE 重装 APK → 重跑启动命令。漏一步，现象就会和代码对不上。

---

## 4. 通信协议

127.0.0.1:19820，纯文本行：

| 命令 | 含义 |
|---|---|
| `b <btn> <0\|1>` | 按钮按下/松开（btn = BTN_* 位号） |
| `a <axis> <值>` | 轴（0..1 浮点） |
| `h <hat>` | 十字键（0=上顺时针到 7=左上，8=中） |
| `t <trig> <值>` | 扳机轴 |
| `r` | 全部复位 |
| `k <usage> <0\|1>` | 键盘键（HID usage，不是 keycode） |
| `?` | 查询版本，回 `VGPAD <n>` |

**关键坑**：app 建 socket 必须声明 `android.permission.INTERNET`，**即使目标是 127.0.0.1**。缺权限时 `new Socket()` 抛 `SocketException: Permission denied`，早年被静默吞掉，表现为「daemon 在 listen，app 显示未连接」。

**守护进程并发**：每个客户端起**独立线程**跑 `serve()`（`GpadDaemon.java`）。原来是本线程直接调 `serve()`，阻塞到客户端断开，`accept()` 根本执行不到——第二个连接只能待在 backlog 里没人接。要命的是 TCP 握手由内核完成，app 那边 `connect()` 成功、看起来"连上了"，但发过去的 `?` 永远等不到回答，2.5 秒后版本号取 -1，界面显示「问不到版本」（见 5.4）。

---

## 5. 排错

### 5.1 浮层左上角状态灯

| 灯 | 文案 | 含义 |
|---|---|---|
| 绿 | 已连接 v5，已发送 N | 正常 |
| 橙 | 连上的是旧进程(vN≠4) | **旧进程没杀干净**，pkill 后重跑 |
| 橙 | 连上的是旧进程（不认识版本查询） | 先看 5.4，多半不是版本旧 |
| 橙 | 守护进程比本 app 新(vN>4) | app 旧了，**重装 app**，别 pkill |
| 红 | 守护进程未连接 — 请先执行启动命令 | 连不上或压根没跑 |

判定加了 `ok`（已连接）前提：没启动时版本号初值 -1，`-1 != 5` 会误报成「旧进程」。

### 5.2 daemon × app 决策表

| daemon 输出 | app 状态条 | 结论 |
|---|---|---|
| 无（执行完即退出） | 红 | 进程被回收（stdin 没读 / 不保活） |
| EADDRINUSE | 红 | 旧进程占位 **+** app 侧问题（多半缺 INTERNET 权限） |
| EADDRINUSE | 绿 | 正常，旧进程在服务 |
| listening + alive | 红 | app 侧：权限 / APK 未重装 |
| listening + alive | 绿，cmds 不涨 | 链路通，游戏没识别设备 |
| listening + alive | 绿，cmds 增长 | 完全正常 |

### 5.3 症状 → 原因 → 处理

- **改了代码没效果**：HID 描述符是守护进程创建 `/dev/uhid` 那一刻注册给内核的，重装 app 不会碰它。必须 pkill → 重装 → 重跑。
- **「重启了」还是旧行为**：A. 旧进程没死透（端口被占时进程**不报错退出**，只是不监听）；B. APK 没重新编译。现在端口占用时会**销毁刚建的设备并 exit 3**。
- **进程起来 1.5 秒就死**：main 线程阻塞在 `accept()` 从不读 stdin，被 Shizuku/Termux 判定空闲回收。现在 main 阻塞在 `System.in.read()`，`accept()` 移到后台线程，每 5 秒打心跳。
- **按键全无反应（史上最致命）**：`SocketLink.connect()` 开头调了 `close()`，里面的 `mSender.shutdownNow()` 把发送线程池关了，后续指令全部被静默丢弃。现在拆成 `disconnect()`（只关 socket）和 `close()`（关线程池，仅服务销毁时调）。
- **某键画得出但点不到**：命中判定区间循环的起止点写小了。**加元素后必须检查所有区间循环**。
- **摇杆/扳机串键**：默认描述符是对的 → 先按 5.2 判断是否旧代码，再 `dumpsys input` 看 `KeyLayoutFile` 是不是 `Generic.kl`，最后 `getevent -l` 看内核原始事件。
- **N 在涨但游戏没反应** = 游戏没识别；**N 不涨** = 触摸没被手柄层收到。

### 5.4 两个新坑（本版新增）

**①「不认识版本查询」多半不是版本旧**

停止 → 重启守护进程后状态点橙色报这个，但两边 `PROTOCOL_VERSION` 明明一致。真因是**守护进程被一个连接卡死**（见第 4 章）。

修法两处：守护进程改成每客户端独立线程（保留"踢掉上一个连接"的逻辑）；握手重试一次（等 300ms 再问）+ 解析放宽（容忍 `VGPAD 4` / `VGPAD4` 这类写法差异，不因一个空格把"问到了"判成"问不到"）。

**② `pkill -f GpadDaemon` 会自杀**

停止命令是 `pkill -f GpadDaemon; ps -A | grep -i gpad`，而执行它的 shell 命令行是 `sh -c "pkill -f GpadDaemon;..."`——**这行本身含 "GpadDaemon"**。`pkill -f` 匹配完整命令行，于是它把自己也杀了，后面的命令没跑完，表现为"守护进程还在"。

```bash
pkill -f [G]padDaemon        # 正则要匹配 "GpadDaemon"，而 shell 行里是 "[G]padDaemon"，对不上
ps -A | grep -i [g]pad       # 同理，免得把自己列出来造成"还在运行"的假象
kill -9 <pid>                # 兜底强杀
```

### 5.5 命令速查

```bash
pm path com.example.vgamepad            # 查真实 APK 路径
pkill -f [G]padDaemon
ps -A | grep -i [g]pad                  # 必须确认无残留
ss -tlnp | grep 19820                   # 端口是否空闲（部分 ROM 无此命令）
dumpsys input | grep -i -B2 -A6 "virtual gamepad"   # 看 KeyLayoutFile
getevent -l                             # 内核原始事件，终极判定
```

期望：`右摇杆 → ABS_Z / ABS_RZ`，`L2 → BTN_TL2`，`R2 → BTN_TR2`。
看到 `ABS_BRAKE/GAS` 或 `Vendor_045e` → 跑的还是旧代码。

---

## 6. 布局与编辑

### 6.1 元素下标

| 区段 | 下标 | 说明 |
|---|---|---|
| 固定区 | 0–21（`N_FIXED = 22`） | 见下表 |
| 键盘槽位 | 22–131（`MAX_KEYS = 110`） | 动态创建，`keyCode[] != 0` = 已占用 |
| 布局按钮 | 132（`I_LAYOUT`，「布」） | |
| 手柄副本槽位 | 133–164（`MAX_PAD_EXTRA = 32`） | 可建第二个 A 键、第二个摇杆 |
| 穿透按钮 | 165（`I_PASS`，「透」） | 默认隐藏 |
| 空白按钮 | 166–181（`MAX_BLANK = 16`） | v5 新增，只占位置不映射任何键 |
| 组合键 | 182–197（`MAX_COMBO = 16`） | v5 新增，一段小宏 |
| 悬浮球 G | 198（`I_FLOAT`） | 本版新增，见 6.3j，`N = 199` |

> **新区段一律追加在数组末尾**，不插中间。存档用下标当 key，插中间会让老存档整段错位；放末尾则 `0..BLANK_START-1` 全不动，直接兼容。

固定区清单：

| 下标 | 名称 | 下标 | 名称 |
|---|---|---|---|
| 0 / 1 | 左摇杆 / 右摇杆 | 11 / 12 | SELECT / START |
| 2 | 方向键 | 13 | 收起 |
| 3–6 | A / B / Y / X | 14 | 编辑 |
| 7 / 8 | L1 / R1 | 15 / 16 | L3 / R3 |
| 9 / 10 | L2(轨) / R2(轨) | 17 / 18 | L2(键) / R2(键)（默认隐藏） |
| 19 | HOME（默认隐藏） | 20 / 21 | Z / C |

> **判断按键身份一律用 `padType` / proto，不能按下标。** 副本槽位上的 L2 下标不是 9，写死 `elem == I_L2` 会全部对不上。

### 6.2 默认布局：横竖是两套设计

| | 竖屏 | 横屏 |
|---|---|---|
| 结构 | 左列：十字键(上) + 左摇杆(下)<br>右列：右摇杆(上) + ABXY(下) | 同左（两列上下叠放，与竖屏同构） |
| 底部基准 | `0.78h` | `0.745h`（比竖屏高，给手势条留余量） |
| 肩键列 x | L 侧 `0.075w` / R 侧 `0.925w` | L 侧 `0.05w` / R 侧 `0.95w`（更贴边） |
| 主区 x | 十字键+左摇杆 `0.28w`；右摇杆 `0.72w`；ABXY `0.76w` | 左列 `0.20w`；右列 `0.76w` |
| SEL/STA | `0.40w / 0.60w`，`0.09h`（顶部） | `0.40w / 0.60w`，`0.88h`（底部） |
| HOME | SEL/STA 正中**下方**（`0.09h + editGap`） | SEL/STA **同一行**中间（横屏高度紧张，不下移） |
| L3/R3 | 肩键排：L3 在 L2 右、R3 在 R2 左 | 同左 |
| 列间距 | 按判定圈反推，非写死比例 | `colGap = max(左列所需, 右列所需) + 2% 短边` |

三条从代码里读出来的经验：
- 竖屏右摇杆 `0.68w → 0.72w`。试过 `0.74w`，在 1600×2560、2048×2732 这类高屏上会撞 R2B（顶部保护区把肩键列压下来，正好和右摇杆判定圈咬在一起）。
- 横屏 `0.79h → 0.745h`：横屏只有 1080 高，太靠底拇指要压到底才能推满，且底下常是手势条。
- HOME 垂直间距用 `editGap`（= `btnR × 2.6`）而不是 `off`（= `btnR × 2.1`）：判定圈都是 1.25r，中心距要 > 2.5r 才不重叠，2.1r 差约 0.4r 会咬在一起。

已知小问题：4:3 平板竖屏（2048×2732）上 L3 与 SELECT 只差 66px，点 L3 右边缘会触发 SELECT。手机上纵向差 156px，不受影响。

### 6.3 编辑模式工具（16 项，按分组）

常用工具菜单按用途分组，显示顺序与常量值解耦（`TL_ORDER`）：

- **重置**：重置全部、重置单个…
- **位置**：互换位置…、**多选…**、回退旧版位置、选中按钮…、网格/吸附…
- **按钮**：隐藏按钮…、删除此键、批量删除…、**按键创建…**、固定显示…

> **「多选调节…」「多选形状…」已从菜单移除**：它们和「多选…」用的是同一份选中集合，
> 拖 / 滑条 / 外观 三种方式都能作用于整批，单列两行只是重复入口（见 6.3b、6.3g）。
- **其他**：布局切换…

> **「关联挪动」已改名为「多选…」，「键盘按键…／手柄按键…」已合并为「按键创建…」**（后者点进去先选类型，再进对应候选表）。`TL_KEY` / `TL_PAD` 常量保留，只是不再直接出现在工具列表里。

几个要点：
- **列表勾选**替代「去画布上点按钮」——按钮会叠、会被拖出屏幕，列表里点名字永远稳
- `pickable()` = 「当它不存在」而非「命中后返回空」，所以隐藏按钮不会吃掉压在下面的键
- **重置单个**可先勾要重置哪些**属性**（原来只能重置位置，想"只把大小调回默认"得整个重置再手摆回去）
- **网格/吸附**：网格只是编辑期辅助线（运行时不画），吸附是松手那一刻把中心对齐到最近网格交叉点或已有按键中心线。默认 8×12
- **固定显示**：配各模板（空白/手柄/键盘）的初始状态。覆盖顺序有讲究——**强制显示必须排在隐藏之前**，否则 `shw` 会被 `hid` 盖掉永远出不来
- **关联挪动用位移量**而非对齐坐标，组内相对位置保持不变
- **重置单个只动位置**，不清掉调好的大小透明度
- 缩放用**对数曲线** `MIN_SCALE * exp(t * range)`，小尺寸段手感细腻

### 6.3b 多选：一个状态管三种批量操作（本版重构）

以前「关联挪动」「多选调节」「多选形状」是**三套各自维护的状态**：关联挪动单独记一份联动组，另两个各开一个批量面板开关。三套互不相通，改了这处容易忘那处。

现在统一成一种状态：**当前选中了哪几个按钮**（`mSelSet`，`GamepadView.java:611`）。单选就是一个，多选就是一批，面板按 `selCount()` 决定显示单键还是批量。

```
怎么选：常用工具 -> 多选… -> 勾几个 -> 确定
        （多选调节 / 多选形状 走的是同一份选中集合）

选中后：拖其中一个            -> 整批跟着走（原「关联挪动」）
        滑条（大小/透明/字体）-> 作用于整批（原「多选调节」）
        更多选项 -> 外观 -> 形状 -> 整批刷成圆形/四边形（原「多选形状」）
```

细节：
- 选中的按钮**画青圈**；「更多选项」在多选时照常显示，不会被批量面板顶掉
- 按在**已选中**的按钮上**不会**取消多选；按在别的按钮上才改成单选它（以前一按就清空，导致批量拖动根本没法用）
- 选中是**会话级、不存盘**：切布局 / 重置 / 退出编辑都会清空
- 多选时「更多选项」里**不给「摇杆设置」**——那是单个摇杆的属性，整批一起设没意义

**两个由此暴露的顺序 bug**（改动这类分派时必看）：

1. `sliderValue()` 里**摇杆设置必须排在 `isMulti()` 之前**（`GamepadView.java:2926`）。以前 `isMulti()` 排最前，多选 + 摇杆设置时读到的是"大小"缓存值；单选因为不走那个分支反而正常——这个差异就是顺序造成的。
2. `clearSelection()` 必须**同时退出子模式**（`mShapeMode` / `mStickCfg`）。留着 `mShapeMode=true` 而 `mSel=NONE`，滑条绘制会去读 `mLayout.widthMul[-1]`，下标 -1 直接崩溃（实测：形状模式下点画布空白处就崩）。

### 6.3c 摇杆：固定 / 浮动 + 范围

| 属性 | 值 | 出处 |
|---|---|---|
| `stickFixed` | 默认 `true`（固定摇杆） | `PadLayout.java:308,791` |
| `stickRange` | 0.5 – 3.0，默认 **1.5** | `PadLayout.java:317-323` |
| 存档字段 | `sf`（固定）、`sr`（范围） | `PadLayout.java:2114-2115` |

- 「更多选项 → 摇杆设置」只有**摇杆**有这一行（`MORE_STICK`），普通按钮不显示——给它们显示"摇杆设置"点了没反应，不如不显示。
- **固定摇杆开着时范围没有意义**，那条滑条不给拖（`GamepadView.java:1984` 直接把滑条数置 0）。
- **关掉固定摇杆后启用「浮动摇杆瞬移」**：手指按在以摇杆中心为圆心、半径 = `摇杆半径 × stickRange` 的圈内 → 摇杆挪到手指位置并存盘；圈外 → 不动。只有摇杆有这个行为（按 proto 判，副本槽位的摇杆也算），十字键 / ABXY 本来就是"按哪儿都行"，瞬移没意义。
- **`hitFloatStick()` 必须存在**：`applyFloatingStick` 只在 `onDown(elem)` 里被调用，而 `elem` 来自 `hitElement()`——那个只认摇杆**本身的几何**。没有 `hitFloatStick` 的话，手指按在摇杆外（但在范围圈内）时 `hitElement` 返回 NONE，根本走不到瞬移逻辑，表现是"预览圈画得老大，实际只有摇杆那一小块按得动，圈是骗人的"。
- 三处口径必须一致：`hitFloatStick`、`radiusOf(i) * stickRange` 的绘制、以及实际判定（`GamepadView.java:2153` 注释）。

### 6.3d 空白按钮（v5 新增）

按键创建 → 空白，生成一个**只有按钮背景、没有名字、按下去什么都不发**的元素。上限 16 个，槽位 166–181。

用途：占位 / 装饰 / 挡住游戏里某一块区域；先摆好位置之后再说放什么；需要一块可拖、可缩放、可透传点击的纯界面元素时。

行为：
- 画布上**不画标签**——它不映射任何手柄键 / 键盘键，摆个名字在上面反而像是漏了什么
- 按下 / 松开**什么都不发**（只有按下时的高亮）
- 除此之外和普通按键一样：能拖、能调大小 / 透明度 / 形状、能隐藏、能在「隐藏按钮」里放出来、也能删

### 6.3e 组合键（v5 新增，本版最大功能）

按一下发出**一段小宏**——手柄键 / 键盘键可以混着放，中间还能插延迟。上限 16 个组合键，每个最多 **32 个动作**。

> 说明文档 `使用说明.txt` 里**完全没有这一节**，以下内容全部从代码读出。

#### 创建与编辑

```
按键创建… → 组合键 → 先选「按钮 / 十字架」
选中它 → 更多选项 → 编辑组合键
```

编辑列表行布局：
```
第 0 行     名字（点一下去改名）
第 1..n 行  每个动作一行（点名字区重选，点右边「删」去掉）
最后一行    「＋ 添加按键」
```
添加时先选「手柄 / 键盘」。选到摇杆会再问「预设（四面八方）/ 高级（自定义 X/Y）」，选到扳机会先问扣多深。延迟行点一下弹档位列表，选完写回那一格。

#### 动作类型（11 种）

| 类型 | 含义 | code 存什么 | tag（第三列）存什么 |
|---|---|---|---|
| 0 | 未设置（空） | — | — |
| 1 | 手柄键按下 | `padType` | 扳机时 = 力度 0–100（老数据 0 当 100） |
| 2 | 键盘键按下 | HID usage | — |
| 3 | **延迟** | 毫秒数 | — |
| 4 | 结束：松开所有已发的键（老数据） | — | — |
| 5 | 结束指定的**手柄**键 | 那个键的编码 | 结束第几个同名键 |
| 6 | 结束指定的**键盘**键 | 那个键的编码 | 同上 |
| 7 | **摇杆推到某个方向** | 摇杆原型下标（`I_LS`/`I_RS`） | 方向号 0–7 |
| 8 | **十字键拨到某个方向** | `I_DPAD` | hat 值 0–7（8 = 回中） |
| 9 | **摇杆回中** | 摇杆原型下标 | — |
| 10 | **十字键回中** | `I_DPAD` | — |
| 11 | **摇杆推到自定义坐标**（高级） | 摇杆原型下标 | `packStick(x,y)` 打包的 X/Y |

**摇杆 / 十字键 / 扳机为什么必须单开类型**：`onButton` 只能表达"按下/松开"，而摇杆要的是轴值（`AXIS_X`/`AXIS_Y`）、十字键走的是 hat 事件。以前选了左摇杆会走 `buttonIndexOfProto` 的 default 分支**发出 `BTN_START`**——既不是摇杆，还是个张冠李戴的键。现在执行端对类型 1 遇到 `I_LS`/`I_RS`/`I_DPAD` 直接跳过（老数据防御）。

**8 向单位向量已归一化**（`DIR_X` / `DIR_Y`）：斜向是 `±0.7071` 而不是 1，否则斜着推合成长度 1.41，比正上下左右"超速"。顺序和 Android hat 值一致：`0=上 1=右上 2=右 3=右下 4=下 5=左下 6=左 7=左上`。

**自定义坐标怎么塞进一个 int**：`tag` 只有一格，各占 8 位——`packStick(x,y)` 把 `-100..100` 偏移成 `0..200`，高 8 位放 X、低 8 位放 Y。

**扳机力度**：用类型 1 + `tag` 存 0–100%，执行时 `onTrigger(idx, pct/100f)`。老存档没设过是 0，当满值处理，行为不变。

延迟档位 12 个：`30 / 50 / 80 / 100 / 150 / 200 / 300 / 500 / 800 / 1000 / 1500 / 2000` ms（`PadLayout.DELAY_PRESETS`）。

**动作表为什么要 3 列**（`类型, 编码, 序号`）：两个 A 的编码完全一样，光靠"类型+编码"分不出停的是哪个。显示时按位置数一遍不可靠——结束行自己占一格，数的时候会把自己的位置算进去，于是永远数成第 1 个（表现："点 A[2] 的结束，建出来却是 结束A[1]"）。所以第三列显式存"结束的是第几个同名键"。

> `MAX_COMBO_ACTS` 从 8 提到 **32** 的理由：组合键的用途不只是"同时按住"，加了延迟之后它更像一段小宏，一条序列里光延迟就能占掉一半格子。改这个数**不影响老存档**——存档按槽位存成 `"类型:编码,类型:编码,…"` 字符串，读时按 j 顺序填回，老存档最多 8 项落在前 8 格，不会错位。

#### 播放语义（关键，做过一次修正）

- **按下启动序列，序列自己跑完再统一松开**。
- 点一下就松手 → 序列照跑，播完自动松开
- 一直按着 → 播完后保持按住，松手才松开
- 无延迟的序列不加收尾，保持原来"按住"的手感，零延迟

> 以前是"松手即中断"：按下 A → 延迟 100ms → B 还没来得及发，手指一抬 `releaseCombo` 就把待发的全取消了，于是"延迟后面的所有键都不触发"。而正常点一下只有几十毫秒，根本等不到延迟走完，**等于延迟功能整个失效**。

- **只松开"已经发出去"的**：延迟中被松手的那些键压根没按下过，补一次松开会让守护进程那边状态错位
- 序列末尾留 `COMBO_HOLD_MS = 60ms`：最后那个键如果"按下即松开"，游戏那一帧可能压根没采样到，表现为"最后几个键没生效"
- **序列播放期间不触发穿透同步**（`GamepadView.java:2247`），否则刚排好的延迟序列会被 `releaseAll` 掐掉
- 快速连点时先 `comboCancelAll()`（`mComboToken++`）收干净再排新的；每个 Runnable 开头比对 token，代号变了说明中途松手或又按了一次，这一发作废

### 6.3f 十字架组合键（一个按钮四条序列）

创建组合键时选「十字架」而不是「按钮」，得到的是一个**十字形控件**：

- 四条臂（上 / 下 / 左 / 右）各有**一条独立序列**，各占一个方向号（`COMBO_DIRS = 4`）。动作表因此多了一维：`(槽位, 方向, 第几个动作, 第几列)`
- 按下时按**落点**决定跑哪条；**斜角默认启用**，开了左上 = 左 + 上 一起触发（关掉就只能点四个正方向）
- 主列表是另一个模式：`名字 / 上 / 下 / 左 / 右 / 启用斜角`。某条方向有内容时，行名后面会带那条序列的概要
- 进某条方向的序列编辑后，列表里**没有「名字」行**——名字在上层改
- **不自动改名**：四条序列各自有键，拼出来的名字又长又没意义，名字只由用户自己定（普通组合键会自动拼 `A+B+延迟…`）

**点亮状态为什么不能用 `mHat`**：`mHat` 是全局唯一的方向键状态，而十字架按下时要按落点**同时点亮两条臂**（斜角），根本塞不进一个 hat 值；而且多个十字架各按各的，共用一个变量会互相覆盖。所以按槽位存位掩码 `mCrossArms`（bit0=上 bit1=右 bit2=下 bit3=左），松手清零。

**自动改名的判据**（普通组合键）：基准是"当前名字 == 上一次我们**自己写进去**的自动名"（`mComboAutoPrev`），**不是**"== 现在算出的自动名"。因为本函数在改完动作之后调用，此刻算出的 auto 已包含刚加的键，拿它和当前名字比必然不等（加 A → 写 "A"；再加 B → 当前是 "A"、auto 是 "A+B"，判成"手动改过"），于是从第二个键起名字就再也不更新了。

### 6.3g 按键外观：底色、改名、形状（本版重组成「外观」子列表）

「更多选项」的第一行现在是**「外观」**，点进去是四项：

```
形状 / 改名 / 改背景颜色 / 按键名
```

> **「外观」永远排第 1 位**：形状已并入外观（它改的也是"这个键长什么样"，单列反而分散）。改名 / 颜色 / 形状 / 按键名是最高频的调节，不管后面再加什么工具（摇杆设置、编辑组合键…），它都固定在第 1 位，不会因为我加行就往下掉。

#### 改背景颜色：12 档固定色 + 两种自定义

主列表仍是 12 档固定色（默认 / 红 / 橙 / 黄 / 绿 / 青 / 蓝 / 紫 / 粉 / 棕 / 灰 / 黑），存档字段 `bc`，0 = 不覆盖走统一配色。想要任意色有两条路：

**① 网页调色盘**（推荐）— 更多选项 → 改背景颜色 → 网页调色盘
**② 手输 `#RRGGBB`** — 兜底，认 `#RRGGBB` / `RRGGBB` / `#RGB`，也认 `#AARRGGBB`

两条都会跳到 app 界面（悬浮窗里弹不出取色器），所以**先确认一次**：游戏会切到后台，可能被系统杀掉。

#### 网页调色盘怎么实现的

Android **没有公开的"取色" Intent**，`android.graphics.Color` 也只有解析方法、没有取色界面，部分厂商 ROM 自带但没有统一 Action 可依赖。但 **HTML 有 `<input type="color">`，由 WebView 提供原生取色面板**——所以借它一用：

```
assets/color_picker.html   （126 行，自包含，无外部依赖）
        ↓ file:///android_asset/color_picker.html?c=RRGGBB&n=3
WebView（setJavaScriptEnabled(true)）
        ↓ window.Android.onColor("#RRGGBB")
@JavascriptInterface → notifyColorSet → setBtnColorHex
```

几个实现要点：

- **初值和目标数量走 URL 查询参数**（`?c=` / `&n=`），比等页面加载完再 `evaluateJavascript` 简单，也不会出现"先闪一下白色再变成当前色"
- **整块放进 `AlertDialog` 而不是 `startActivity`**：复用现有的 `finishRename()` 收尾逻辑（改完把悬浮窗放回来），不用新开 Activity、不用在 manifest 注册、不用处理返回栈
- **JS 回调不在主线程**，`onColor` 里要 `runOnUiThread` 再回写
- 取消 / 返回都要 `wv.destroy()`，否则 WebView 泄漏
- 页面上那句提示写的是「确定后应用到 N 个按键」而不是重复说明"点色块能取色"——后者就是页面上那个控件本身，再说一遍是废话
- 提示放在色块**上方**、箭头朝下 `↓`：横排时文字紧贴色块，「点此按钮选择 ←」里的"此按钮"容易被读成指这句提示本身

#### 取色器初值不能一律给白

界面按钮（编 / 布 / 收 / 透）的底色是**几支专用画笔写死的**，不经过 `btnColor`，所以 `btnColor` 是 0（= 没设过）。一律回退白色的话，打开取色器看到 `#FFFFFF`，和屏幕上实际显示的紫 / 红 / 绿对不上，改一点就跳成另一个色。

现在走 `defaultColorOf(i)`：没设过自定义色时，返回这个元素**实际显示**的颜色。

#### 穿透按钮有两支颜色

`mColorPassState`：0 = 改普通 `btnColor`；1 = 改穿透**开**时的颜色；2 = 改穿透**关**时的颜色。穿透那两支不是"选中的键"，写的是全局字段，所以取色前会先弹 `LIST_PASS_COLOR` 让你挑设哪一支。

**单个按键改名**：和布局改名 / 组合键改名一样，悬浮窗里弹不出输入法，要跳 app 界面，先确认一次。

**按键名显示/隐藏**：「外观 → 按键名」控制画布上画不画标签（不影响身份）。

### 6.3h 列表里的可拖调节条（本版改进）

扳机力度和摇杆坐标原来只能点 `−/+` 一步步调，太慢。现在直接在**条 / 盘**上点一下或拖一把：

- 扳机力度条：`扣下 N%`
- 摇杆预览盘：可在盘上直接拖到想要的坐标，带预览
- 几何存在内容坐标里（和 `mListItemRects` 同一套），命中时要减掉 scroll
- `mListAdjustPointer` 记录正在拖的是哪根手指，避免第二根手指乱入把值带跑

### 6.3i 横竖屏同步

> 说明文档 `使用说明.txt` 里没有这一节，以下内容全部从代码读出。

入口在**布局列表每一行**的「同步」按钮。
从右往左排：**分享 | 同步 | 删 | 改名 | 名字**，即左到右读作「改名 删 同步 分享」。
**内置布局只给 分享 + 同步**（删 / 改名点了只会弹提示，摆出来是按了没反应的两颗）；
不给的那两格**矩形必须 `setEmpty()`**，光是不画不够——矩形还在的话点空白照样触发。

三步走：
```
① 选方向     从横屏复制到竖屏 / 从竖屏复制到横屏
② 勾哪些按钮
③ 勾要复制哪些属性   大小复制 / 透明复制 / 字体复制 / 其他信息
```
「其他信息」= `sp` 形状、`h` 隐藏、`sf`/`sr` 摇杆固定与范围、`wm`/`hm` 宽高倍率。

三处设计要点：

- **位置可以有条件地复制**。默认不复制（横竖比例不同，搬过去会跑到屏幕外），但**目标方向压根没有这个按钮的记录时**沿用源方向的位置当落点——完全不给位置的话 `rx/ry` 会留 0，按钮飞到左上角，比复制过去还难找。
- **身份字段强制跟着走**。`pt`（padType）/ `k`（keyCode）/ `bl` / `cn` / `ca` 决定"这个槽位上是什么按钮"，**不受勾选管，一律复制**。只复制大小透明度的话目标方向那个槽位还是空槽位，用户建的键盘键、空白、组合键根本不会出现，看着就像"同步了但没复制过来"。
- **方向必须显式选，不能反推**。反推的话用户不知道自己点了之后往哪边搬；而且当前方向**没有存档**时，反推出来的方向压根无数据可复制。

> 修过一个方向反了的 bug：原来签名是 `portrait = 目标`、内部 `srcPortrait = !portrait`，而调用方手上拿的是"源是不是竖屏"，传进去被当成目标——**被改的反而是竖屏**。现在取消反推，参数直接给源。

### 6.3j 悬浮球 G 有了自己的槽位和布局（本版新增）

**新槽位 `I_FLOAT = 198`**（数组末尾，`N = 199`），**新内置布局「悬浮窗」`LAYOUT_FLOAT = 31`**——专门调悬浮球（G）的样式。

**为什么 G 必须独立成槽位，不能复用 `I_COLLAPSE`**：复用等于"「收」在悬浮窗布局里改个名字换个色"，于是——

- 点**全部重置**会把它打回「收」（`reset` 按模板重建界面按钮），用户改的蓝底白字 G 全没了
- G 和「收」**共用一套 scale / 颜色 / 名字**，改一个另一个跟着变，而它们本该是不同的东西

独立槽位之后 G 有自己的外观存档，重置也只会重置回"默认的 G"。

**为什么 `LAYOUT_FLOAT` 取 31 而不是 2**：id 必须 `< LAYOUT_USER_START`（=2）才算内置，但 2 已经被"第一个用户布局"占了几百个版本——挪 `LAYOUT_USER_START` 会让老用户的 `id=2` 布局被当成悬浮窗布局，存档直接串味。取一个大号（远在用户布局号段之外）就没这个冲突：用户布局从 2 递增，`nextLayoutId` 会跳过这一号。

**G 的位置恒等于「收」**：`l.rx[I_FLOAT] = l.rx[I_COLLAPSE]`（`:2878`）。另有一个 `applyFloatVisibility(l)`，否则会出现"切到悬浮窗布局看不见 G"或"手柄布局上多出一颗球"。

**悬浮窗布局里的四件事**：

- **提示牌**：一块空白按钮改成长方形横条（宽高倍率 `7.04 × 1.95`），文案
  `本布局修改“G”会同步修改缩小之后的“G”`，摆在屏幕正中（顶部是界面按钮、右上是 G，中间是空的）。
  按**前缀**「本布局修改」认牌而不是全等——文案会改，写死全等的话改一次文案，老存档就认不出已有那块，
  load 一次补一块，16 个空白槽位很快堆满。找到旧牌子就地改文案，不新建。
- **「布」「透」在这个布局里都显示**：「布」是切走的唯一出口（藏了就把自己锁在里面），「透」是全局开关。
- **「布」开的快速切换列表不列悬浮窗布局**（`LIST_LAYOUT_PICK` 过滤），但**常用工具 → 布局切换照常列出**，否则再也进不去。
- **常用工具只剩 5 项**（`FLOAT_TOOLS`）：重置全部、重置单个…、选中按钮…、网格/吸附…、布局切换…。
  互换位置 / 多选 / 隐藏 / 批量删除 / 按键创建 / 固定显示 在一颗球面前全是点了没反应的死项。
  过滤只作用于 `LIST_TOOL` 且当前是悬浮窗布局，别的布局一项不少。

> 分类常量也拆了：`CAT_COMBO`（组合键，含十字架）、`CAT_UI`（功能键 = 编/布/收/透）各自独立；布局列表只有 4 项模板分类（`CAT_MAX_LAYOUT`），**没有「组合」「功能」**。以前靠"哪个列表在用"来解释下标含义，现在不再共用。

### 6.3k 布局调节：把一片键当整体缩放（本版新增）

多选后调节面板上有个**「布局调节」**开关：

- **关**：大小滑条 = 原地改各自尺寸，位置不动，间距也不动
- **开**：大小滑条 = **整体缩放倍率** `k`——按钮连同彼此的间距一起朝中心按钮收缩 / 放大，等价于把这一片当成一个整体来缩放

- 倍率区间 `ADJ_K_MIN = 0.35` ~ `ADJ_K_MAX = 1.60`（下限留到 0.35 才够把一片键压得很紧）
- **中心按钮（锚点）**在已勾选的那批里单选——中心必须是参与者之一，拿一个没参与缩放的按钮当锚点没有意义。它自己位置不动，其余按钮按倍率向它聚，所以**它决定了这一片缩完之后停在哪**
- 开启那一刻会快照 `rx / ry / scale`（`mAdjSnapRx` 等），`k` 永远相对它算
- 「中心：XX」按钮只在布局调节打开时才存在，关着的时候要 `setEmpty()`，否则是幽灵按钮

> 另一处相关修正：三条滑条在非布局模式下会**缓存"统一刷成多少"**（`mAdjSizeVal` 等）。不缓存的话滑条值取自第一个选中元素，某个元素被单独改过之后下次进来滑条会跳到那个元素的值上，看着像"面板没记住我上次的设置"。

### 6.4 布局管理（多套 + 导入导出）

- 布局有 id + 名字 + 模板（`TPL_PAD` / `TPL_KEYBOARD` / `TPL_BLANK`），内置「默认手柄 / 默认键盘」，可新建、改名、切换、删除
- 「布」按钮进的列表**只能切换**；常用工具进的可改名 / 删除（避免误触）
- **导出（分享）**：把一套布局的**竖屏 + 横屏两份**摆位存成一个 JSON 到 Download，文件名 `布局名_年_月_日_时_分_秒.json`（时间戳保证同名多次导出不覆盖）
- **导入**：跳到 app 界面选文件。会先确认——游戏切到后台可能被系统杀掉
- 竖屏 / 横屏各存一份，互不影响
- **导入时收成悬浮球而不是设 GONE**（`FloatingService.java:833`）：选文件要离开游戏，一路黑屏会让人以为悬浮窗崩了。收成球看得见点得到；而且 `hidePad()` 会销毁 `GamepadView`，回来点球重新展开 = 新建 View，布局列表自然重新读盘，刚导入那套一定在列表里，不用手动刷新。改名 / 搜索仍走 `mPadHiddenForRename`（GONE）——那两个是"原地改完接着用"，窗口状态得留着。
- **导入后回不到游戏的坑**（`MainActivity.java`）：弹系统文件选择器后本界面被盖住 → `onStop`。而 `onStop` 原本的逻辑是"界面看不见了就恢复悬浮窗并清标记"，会把 `sRenameFromPad` 清成 false，等 `onActivityResult` 调 `finishRename()` 时开头 `if (!sRenameFromPad) return;` 直接 return——`moveTaskToBack` 没执行，人留在主界面，看起来"没返回游戏"。现在用 `sAwaitingImportResult` 挡住这次提前收尾。
- 另有 `sResumed` 判断"这次跳转是不是从游戏里发起的"：跳转时本界面**已在**前台 → 用户本来就在 app 里，完成后不该退回游戏（那等于退出软件）；不在前台 → 是从悬浮窗跳过来的，完成后该退回。

### 6.5 存档与版本号规则

| 场景 | 要不要升版本 |
|---|---|
| 新增元素，且 `load()` 会带出 reset() 默认值 | **可以不升**（老存档没这个 key → continue → 保留默认位置和隐藏态，不清理用户布局） |
| 改了默认摆放坐标 | **必须升**，否则用户存的旧 x/y 会盖回来 |
| 改横竖共用的逻辑 | 横竖都升 |

代价与收益权衡：升版本 = 用户手调的布局作废。「加一个默认隐藏的元素」优先选不升。

### 6.6 屏幕适配要点

- 位置按**相对宽高比例**存，换设备、转屏都不错位；横竖各一套
- 列间距按**判定圈反推**，不写死比例
- 判定倍率（1.15/1.35/1.25）与 `hitElement()` 里写死的必须一致，改一处漏一处就会重叠
- 编辑模式收边用**完整半径 + 余量**
- 摇杆模拟范围**改成圆形**（模长归一）：方框会让斜推合成 1.414，斜跑超速
- 顶部保护区约 3.5% 屏高；底部一律不限制
- 已验证：历代累计覆盖 19 种比例（含 4:3 平板、1600×720 矮横屏）

---

## 7. 编译与环境约束（别改的地方）

- **targetSdk 保持 28**：30+ 包可见性限制、31+ 禁后台启动前台服务、34+ 强制声明 `FOREGROUND_SERVICE` 类型（悬浮球起不来）。本工程靠的正是前台服务 + `SYSTEM_ALERT_WINDOW`。
- **compileSdk 必须 33**：Shizuku 13.1.5 的 AAR 按 33 编译。两者是不同概念，不一样是故意的。
- **不要加 `<queries>`**：老 aapt 不认识这个 API 30 元素 → 资源编译失败 → R.java 不更新 → 只有**新加的 id** 报 `Unknown member`。targetSdk 28 本来就不需要它。遇到就 Clean/Rebuild 或删 `app/build`。
- **Shizuku 依赖三要点**：版本 13.1.5（12.2.0 在 google()+mavenCentral() 拉不到）；只要 google() + mavenCentral()；`aidl` 行要显式写（AIDE 未必解析传递依赖）。
- **`newProcess` 是 private**，必须 `getDeclaredMethod + setAccessible(true)`；`ShizukuRunner` 做了三层签名降级，全失败会把实际签名打到日志。
- **绝不 `waitFor()`** 守护进程；`exec()` 起进程后 stdout/stderr **两个流都要抽**（只抽一个，另一个写满会憋死子进程）。
- **provider authority 唯一**：只保留官方 `rikka.shizuku.ShizukuProvider`。同时声明手写 `.ShizukuProvider` 会 `INSTALL_FAILED_CONFLICTING_PROVIDER`。
- **「查不到 Shizuku」≠ 没装**：国产 ROM 另加「读取应用列表」管控，不看 targetSdk，返回的同样是 `NameNotFoundException`，代码无法区分。用「打开 Shizuku」按钮交叉验证。
- **为什么不能全自动**：要 app 自己以 shell(uid 2000) 跑命令，标准做法是 Shizuku SDK（含 AIDL，AIDE 编不了）。换到 Android Studio 或配好 SDK 的 Termux 后可改 UserService 架构，连守护进程和 socket 都能删掉。

---

## 8. 已被推翻的历史结论（档案，勿复用）

| 旧结论 | 为什么错 / 现状 |
|---|---|
| 右摇杆用 Rx/Ry（xpad / Xbox 约定） | 那是 PC/Linux 桌面约定。Android CDD 规定右摇杆 = **Z/Rz**，扳机 = Brake/Accelerator。旧结论恰好反了 |
| 描述符 87 字节，Usage 顺序 X Y Rx Ry Hat Z Rz | 已改为 89 字节 CDD 布局 |
| VID/PID = `045E:028E`（Xbox 360） | 会引来 `Vendor_045e_Product_028e.kl`，该文件**没有 312/313 两行** → L2/R2 哑；还把 Z/Rz 重映射成扳机，掩盖了上一行的错误。现用 `1D6B:0104` |
| app 里的版本提示能判断 APK 有没有更新 | 不能。两者 `PROTOCOL_VERSION` 在同一个 APK 里，天生相等。**看到橙点 = 旧进程没杀干净；看不到橙点 ≠ APK 是新的** |
| 端口占用时守护进程继续运行 | 会留下「僵尸手柄」。现在销毁设备并 exit 3 |
| 自检摇杆画圈默认开启 | 太像故障，改为 `--selftest` 才触发 |
| 编辑模式底部也限制按键 | 改为：底部一律不限制，只有顶部保护区 |
| 隐藏按钮命中后 `return NONE` | 会吃掉点击，压在下面的键也点不动。改为 `pickable(i)` 跳过 |
| `localhost` 不需要 INTERNET 权限 | 恰恰相反，Android 上建 socket 必须声明 |
| 键盘按键上限 16 个 / 键名表 32 项 | 现 `MAX_KEYS = 110`，键名表约 90 项（全字母 + F1~F12 + 小键盘） |
| 横屏两个摇杆上下叠放会重叠 | 错误，那成立的前提是旧布局的横向比例。改两列后 1080 高的横屏约需 790px，放得下 |
| 命中判定用写死下标区间 | 已重构成按 proto 全量遍历，副本槽位问题从根上解决 |
| 「关联挪动 / 多选调节 / 多选形状」三套独立状态 | 已合并成一种「选中集合」（6.3b）。工具项「关联挪动」改名「多选…」 |
| 「键盘按键… / 手柄按键…」两个并列入口 | 合并为「按键创建…」（`TL_CREATE`），进去先选类型 |
| 摇杆只能固定位置 | 新增 `stickFixed` / `stickRange`，可关掉固定启用浮动瞬移（6.3c） |
| 键盘 boot 协议 6 键无冲 | v5 改 NKRO bitmap（30 字节），无上限；`--kbd-boot` 可回退（3.3） |
| 组合键只能靠手速 | v5 新增组合键（6.3e）：16 槽 × 32 动作，含延迟，本质是一段小宏 |
| 组合键"松手即中断" | 改为序列跑完再统一松开——否则延迟后面的键永远不触发（6.3e） |
| 组合键只能发"按下/松开" | v5 补了摇杆推方向 / 十字键拨方向 / 扳机力度 / 自定义坐标（6.3e） |
| 「更多选项」按写死下标比对 | 改成 `moreRowNames()` 合成一份表、点击按**名字**匹配（行数是变的，写死下标会点空） |
| 自动改名按"当前名 == 刚算出的自动名"判断 | 基准改成 `mComboAutoPrev`，否则第二个键起名字再也不更新（6.3f） |
| 取色只能靠 12 档固定色 | 加 `assets/color_picker.html` + WebView 原生取色器（6.3g），另有手输 #RRGGBB 兜底 |
| 悬浮球 G 复用 `I_COLLAPSE` | 独立槽位 `I_FLOAT` + 独立布局 `LAYOUT_FLOAT`（6.3j） |
| 「更多选项」里形状单列 | 并入「外观」子列表，且外观永远排第 1 位（6.3g） |

---

## 9. 仍然缺失的能力

按痛感排序，这些代码里确实没有：

1. **撤销 / 重做** — 代码注释自己承认「删掉的键没法撤销，先确认」。用确认框替代 undo 是权宜之计：确认框只能防误点，防不了"我想试试这样摆，不行再退回来"。本版多选重构后，`clearSelection()` 这类收尾入口已经很干净，加撤销栈的落点清晰。
2. **编辑期试玩** — `isPassThroughActive()` 要求 `!mEditMode`，编辑期间必须吃掉全部触摸。调完一个键得退出才知道手感，不满意再进编辑，来回割裂。缺的是第四态：保留编辑布局但允许穿透试按。这条现在更痛了：**组合键调完必须退出才知道延迟对不对**，而延迟是最需要反复试的参数。
3. **布局内搜索** — 按钮多了以后（键盘布局 80+ 个键）找某一个靠肉眼扫。`LIST_*` 里有通用筛选，但布局列表不走它（`listUsesGenericFilter()` 返回 false），怕两套下标打架。
4. **z-order 可调** — 重叠时谁在上靠硬编码优先级。
5. **精确数值输入** — 想让两键完全同大，只能两条滑条都拖到同一百分比。
6. **组合键循环 / 连点** — 序列播完即止，没有"重复 N 次"或"按住时循环"。连点场景得靠手速。这是组合键目前唯一的缺口（摇杆 / 十字键 / 扳机本版已补齐）。

另外，之前提过的**布局导入导出（6.4）、网格吸附（6.3）、宏（6.3e 组合键）、NKRO（3.3）、横竖屏同步（6.3i）都已实现**。

> 顺带一提：`stickRange` 默认 1.5 意味着摇杆的**判定/激活范围是半径的 1.5 倍**，但 `hitElement()` 里摇杆用的判定倍率是写死的 1.35。这两处口径目前各管各的（一个管"浮动瞬移的激活圈"，一个管"常规命中"）。如果以后调默认范围，注意别让预览圈和可按区域对不上——这正是 `hitFloatStick()` 那段注释想防的。

---

## 附录：版本号变更记录

| 版本 | 改动 | 是否重置用户布局 |
|---|---|---|
| 5 | 新增 L3/R3 + 布局整体下移 | 是（横竖共用） |
| 6 | 横屏重排：L3/R3 归肩键排、SEL/STA 移底部中间 | 重置横屏 |
| 7 | 横屏改两列上下叠放 | 重置横屏 |
| 8 | 面板三档收起 + 三个新工具 + 横屏左列 0.24w→0.20w | 重置横屏 |
| 9 | L2/R2 改竖直滑轨，肩键列下移 | 重置横屏 |
| 10 | 摇杆范围改圆形 + B/X 对调 | 横竖都重置 |
| 11 | 竖屏右摇杆 0.68w→0.72w | 重置竖屏 |
| — | 新增 L2(键)/R2(键)（→19）、HOME（→20）、Z/C（→22） | **不升版本，老布局保留** |
| 13 / 14 | 当前（横 / 竖）。含横屏底部 0.79h→0.745h、I_LAYOUT/I_PASS 新增 | 是 |
| — | v5 新增空白按钮（166–181）、组合键（182–197） | **不升版本**，新区段追加在末尾，老存档下标全不动 |

### 协议版本记录（与布局版本是两套，别混）

| 协议 | 改动 |
|---|---|
| 3 → 4 | 新增键盘设备（第二个 uhid） |
| 4 → 5 | 键盘 boot 6KRO → NKRO bitmap（30 字节）。**破坏性**：旧进程收到新 usage 会照旧往 6 格里塞 |

# 虚拟手柄 · VirtualGamepad

一个跑在 Android 上的虚拟手柄。

在屏幕上叠一层可自定义的虚拟按键，把点按转换成标准 Linux 输入事件
（经由 `/dev/uinput`），让系统像对待真实手柄一样处理。

- 演示视频：[哔哩哔哩](https://b23.tv/TqaFaQV)
- 作者 B 站：https://b23.tv/r6uKjwj

---

## 下载

| 渠道 | 地址 |
|---|---|
| 123 网盘 | https://1834362934.share.123pan.cn/123pan/6OZQTd-K23hH |
| GitHub Releases | [最新版](https://github.com/3994305027/VirtualGamepad/releases/latest) |

App 内「⋯ → 检查更新」会自动比对版本并给出这两个入口。

---

## 运行条件

- Android 8.0（API 26）及以上
- 平时不需要联网；进入时会静默检查一次更新（默认开，可在「⋯ → 设置」关闭）

需要以下**任一**授权：

- **Shizuku**（通过 root 或无线调试激活）
- **ADB**：无线调试 / 电脑 adb / Termux / LADB
- **root**

守护进程以 `uid 2000`（adb shell）或 `uid 0`（root）运行，
两者都行 —— Shizuku 不是唯一途径。

---

## 功能

**布局**
- 多布局系统：默认手柄、默认键盘、自定义布局
- 每个布局独立保存，带创建时间
- 布局可导出 / 导入 / 分享

**编辑**
- 拖动改位置，带吸附对齐
- 大小、透明度、字体大小可调
- 背景颜色自定义：预设色板 / 网页取色 / 手动输入 `#AARRGGBB`
- 透明度支持百分比与十进制两种单位，可点击切换

**穿透**
- 触摸穿透开关，开 / 关两套独立配色
- 采用 `touchableRegion` 区域方案

**其他**
- 收起成悬浮小球，再展开
- 组合键：动作序列、方向、触发方式、摇杆模式、延迟
- 主界面 UI 元素可隐藏
- 内置更新日志，升级后只提示新增部分
- 进入软件时静默检查更新（默认开，可关）：不弹加载框、不挡操作，
  仅在标题旁短暂显示「正在检查更新…」，检测到新版才弹窗

---

## 编译

```bash
# 版本号（格式 年.月.日.时.分）
sh 改版本号.sh                # 用当前时间
sh 改版本号.sh 26.9.25.10.0   # 用指定时间

# 构建
./gradlew assembleRelease
```

要求：
- `compileSdkVersion 33`（Shizuku API 13.1.5 的 AAR 按 33 编译）
- `targetSdkVersion 28`（刻意保持，见下）

---

## 技术说明

**输入注入**：经 `/dev/uinput` 写入标准 Linux 输入事件，
与真实外设走同一条链路，不走无障碍服务。

**targetSdk 为什么固定 28**：
升到 30+ 会触发包可见性限制、后台启动前台服务受限等问题，
而本工程依赖的正是前台服务 + 悬浮窗，故刻意保持 28。

**触摸穿透**：
采用 `WindowManager.LayoutParams.touchableRegion` 区域方案，
而非某些方案中"压低窗口 alpha"的做法，
因此不受 Android 12+ / 16 上 alpha 限制策略变化的影响。

**检查更新**：
依次尝试 GitHub Releases API → jsDelivr 镜像 → raw。
Release 说明里写一行 `versionCode: 384302` 用于比对；
另写 `网盘: <地址>` 可指定网盘入口。

默认在 `onResume` 时静默执行一次（不弹加载框），
也可由用户在「⋯ → 设置」里手动触发或关闭自动检查。

---

## 开源依赖

- **Shizuku** 13.1.5 — [GitHub](https://github.com/RikkaApps/Shizuku)
  Apache License 2.0

未修改 Shizuku 任何源码，仅调用其接口。

完整许可证、依赖清单与免责声明见根目录 `LICENSE`、`NOTICE`、`DISCLAIMER.md`，
也可在 App 内「⋯ → 关于 / 开源许可」中查看。

---

## 隐私

本产品的网络行为仅限两项：

1. **本机回环**（127.0.0.1）Socket 通信 —— 守护进程与 UI 之间的通道
2. **检查更新** —— 向本仓库发起一次 HTTPS GET，读取版本信息

检查更新有两种触发方式：

- **a. 用户主动点**「⋯ → 检查更新」
- **b. 进入软件时自动静默检查一次** —— 默认开启
  首次使用的协议弹窗里会明确列出并可取消勾选；
  之后可在「⋯ → 设置」中随时关闭

两种方式都只在检测到新版本时才弹窗，
已是最新或查不到则完全静默（标题旁短暂显示「正在检查更新…」）。

除上述两项外，不连接任何其他服务器，不上传任何数据，
不含任何第三方统计 / 广告 SDK。

不手动点击、且关闭自动检查后，不会产生任何对外网络请求。

---

## 免责声明

本工具**仅做输入事件映射**：

- 不读取、不修改任何游戏的内存或数据
- 不注入代码、不 Hook、不拦截网络
- 不提供脚本、宏、连点或任何自动化能力
  —— **每一个输入事件都对应你的一次真实点按**

是否允许使用此类外部输入设备映射工具，
请以你所玩游戏的官方用户协议为准。
用于联网对战、排位或涉及虚拟财产的场景前，请先自行确认。

详见 `DISCLAIMER.md`。

---

## 关于开发方式

```
软件：AI 从零写的
简介：AI 写的
测试：我
```

本项目的代码由 AI 编写；
需求设计、真机测试与验收由本人完成。
